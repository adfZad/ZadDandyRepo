import { NextResponse } from "next/server"; import { prisma } from "@/lib/prisma"; export const dynamic = "force-dynamic"; export async function GET() { try { const hasDbUrl = !!process.env.DATABASE_URL; let dbStatus = "Not tested"; let dbError = null; if (hasDbUrl) { try { const count = await prisma.supervisorMaster.count(); dbStatus = `Connected successfully! Found ${count} supervisors.`; } catch (e: any) { dbStatus = "Connection failed"; dbError = e.message || String(e); } } return NextResponse.json({ status: "Running", databaseUrlSet: hasDbUrl, dbStatus, dbError }, { status: 200 }); } catch (error: any) { return NextResponse.json({ status: "Fatal Error", error: error.message || String(error) }, { status: 500 }); } }
